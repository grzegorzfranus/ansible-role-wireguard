---
- name: Prepare
  hosts: instance
  become: true
  tasks:
    - name: Ensure APT package cache is updated (Debian/Ubuntu)
      ansible.builtin.apt:
        update_cache: true
        cache_valid_time: 3600
      when: ansible_facts['os_family'] == 'Debian'
