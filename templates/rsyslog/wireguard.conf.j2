#jinja2: trim_blocks: True, lstrip_blocks: True
# This file is managed by Ansible - do not edit manually
# RSyslog configuration for WireGuard service

# RSyslog configuration for WireGuard service
# This template configures rsyslog to redirect WireGuard logs to a dedicated file
# for better log organization and monitoring.

# Redirect WireGuard logs to dedicated file
# Match by explicit SyslogIdentifier, wg-quick program name, or unit syslog tag
if (
  $programname == '{{ wireguard_syslog_identifier }}' or
  $programname == 'wg-quick' or
  $syslogtag startswith 'wg-quick@{{ wireguard_interface }}['
) then {{ wireguard_log_file }}

# Stop processing these logs further to prevent duplication
& stop
